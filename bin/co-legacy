#!/usr/bin/env node
import { spawnSync } from "node:child_process";

const legacyEntrypoint = "/Users/designc/Documents/cyber-office/bin/co";
const result = spawnSync(legacyEntrypoint, process.argv.slice(2), {
  env: process.env,
  stdio: "inherit",
});

process.exit(result.status ?? 1);
